# Akash-API-Data-Viewer


A modern, responsive, and animated web application built with **HTML, CSS, and JavaScript** that fetches user data from a public API and displays it in a clean, card-based UI.  
This project demonstrates **Fetch API**, **asynchronous JavaScript**, **error handling**, **JSON parsing**, and **responsive design**.

---

## 🚀 Features
- **Fresh Modern Color Theme** – clean, professional look with subtle gradients.
- **Fetch API Integration** – retrieves data from [JSONPlaceholder](https://jsonplaceholder.typicode.com/users).
- **Live Search** – filter users instantly by name or email.
- **Sorting Options** – sort data alphabetically by name or email.
- **Responsive Grid Layout** – adapts to mobile, tablet, and desktop.
- **Hover Animations** – smooth scaling and shadow effects on cards.
- **Loading Spinner** – displays while data is being fetched.
- **Error Handling** – shows an error message with retry button when offline or API fails.
- **Reload Button** – instantly refreshes data.
- **Smooth Fade-in Animations** – data appears elegantly after loading.

---

## 📂 Project Structure
Akash-API-Data-Viewer/
│── index.html # Main HTML file
│── style.css # Styles for layout, colors, and animations
│── script.js # JavaScript logic for fetching and displaying data
└── README.md # Project documentation

---

## 🛠 How We Built It

1. **HTML Setup**
   - Structured a header with title, search bar, sort dropdown, reload button, and a container for user cards.

2. **CSS Styling**
   - Applied a **fresh color palette** for a modern and clean interface.
   - Used **CSS Grid** for responsive layouts.
   - Added hover effects, shadows, and smooth transitions.
   - Designed a **loading spinner** and error screen for better UX.

3. **JavaScript Functionality**
   - Used **Fetch API** to get user data from `https://jsonplaceholder.typicode.com/users`.
   - Parsed JSON and dynamically generated user cards.
   - Implemented **live search** and **sorting**.
   - Added **error handling** with retry button functionality.
   - Used animations for smooth content rendering.

4. **Responsiveness**
   - Media queries make the design work perfectly across devices.
   - Mobile-friendly card stacking for better readability.

---

Live Site: https://akarv742006.github.io/Akash-API-Data-Viewer/
